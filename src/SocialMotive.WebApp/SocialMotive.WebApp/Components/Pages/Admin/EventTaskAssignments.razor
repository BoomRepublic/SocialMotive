@page "/admin/event-task-assignments"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Event Task Assignments</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Event Task Assignments Management</h1>
        <p class="text-muted">Manage participant task assignments</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/event-task-assignments (paginated, sortable, filterable)
         - Create: POST /api/admin/event-task-assignments with EventTaskAssignmentDto
         - Update: PUT /api/admin/event-task-assignments/{id} with EventTaskAssignmentDto
         - Delete: DELETE /api/admin/event-task-assignments/{id}
         - Export: CSV/Excel export
         - Grid Columns: AssignmentId, EventTaskId, EventParticipantId, AssignedDate, Status, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: EventTaskId required, EventParticipantId required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
