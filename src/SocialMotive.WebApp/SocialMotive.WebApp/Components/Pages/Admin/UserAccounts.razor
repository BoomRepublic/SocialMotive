@page "/admin/user-accounts"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - User Accounts</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>User Accounts Management</h1>
        <p class="text-muted">Manage user account details and preferences</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/user-accounts (paginated, sortable, filterable)
         - Create: POST /api/admin/user-accounts with UserAccountDto
         - Update: PUT /api/admin/user-accounts/{id} with UserAccountDto
         - Delete: DELETE /api/admin/user-accounts/{id}
         - Export: CSV/Excel export
         - Grid Columns: AccountId, UserId, AccountType, ExternalId, Status, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: UserId required, AccountType required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
