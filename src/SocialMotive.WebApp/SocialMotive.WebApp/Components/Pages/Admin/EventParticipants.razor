@page "/admin/event-participants"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Event Participants</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Event Participants Management</h1>
        <p class="text-muted">Manage participant registrations</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/event-participants (paginated, sortable, filterable)
         - Create: POST /api/admin/event-participants with EventParticipantDto
         - Update: PUT /api/admin/event-participants/{id} with EventParticipantDto
         - Delete: DELETE /api/admin/event-participants/{id}
         - Export: CSV/Excel export
         - Grid Columns: EventParticipantId, EventId, UserId, RegistrationDate, Status, HoursLogged, Review, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: EventId required, UserId required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
