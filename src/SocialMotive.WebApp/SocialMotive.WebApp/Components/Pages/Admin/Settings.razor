@page "/admin/settings"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Settings</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Settings Management</h1>
        <p class="text-muted">Manage system settings and configuration</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/settings (paginated, sortable, filterable)
         - Create: POST /api/admin/settings with SettingDto
         - Update: PUT /api/admin/settings/{id} with SettingDto
         - Delete: DELETE /api/admin/settings/{id}
         - Export: CSV/Excel export
         - Grid Columns: SettingId, Key, Value, Description, DataType, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Key required, Value required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
