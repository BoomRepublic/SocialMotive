@page "/admin/events"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Events</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Events Management</h1>
        <p class="text-muted">Manage volunteer events</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/events (paginated, sortable, filterable)
         - Create: POST /api/admin/events with EventDto
         - Update: PUT /api/admin/events/{id} with EventDto
         - Delete: DELETE /api/admin/events/{id}
         - Export: CSV/Excel export
         - Grid Columns: EventId, Name, Description, StartDate, EndDate, Location, Status, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Name required, StartDate required, EndDate required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
