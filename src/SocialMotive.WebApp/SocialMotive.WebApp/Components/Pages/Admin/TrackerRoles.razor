@page "/admin/tracker-roles"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Tracker Roles</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Tracker Roles Management</h1>
        <p class="text-muted">Manage roles within tracker hierarchy</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/tracker-roles (paginated, sortable, filterable)
         - Create: POST /api/admin/tracker-roles with TrackerRoleDto
         - Update: PUT /api/admin/tracker-roles/{id} with TrackerRoleDto
         - Delete: DELETE /api/admin/tracker-roles/{id}
         - Export: CSV/Excel export
         - Grid Columns: RoleId, TrackerId, Name, Description, Permissions, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Name required, TrackerId required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
