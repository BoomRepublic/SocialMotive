@page "/admin/groups"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Groups</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Groups Management</h1>
        <p class="text-muted">Manage volunteer groups</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/groups (paginated, sortable, filterable)
         - Create: POST /api/admin/groups with GroupDto
         - Update: PUT /api/admin/groups/{id} with GroupDto
         - Delete: DELETE /api/admin/groups/{id}
         - Export: CSV/Excel export
         - Grid Columns: GroupId, Name, Description, MemberCount, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Name required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
