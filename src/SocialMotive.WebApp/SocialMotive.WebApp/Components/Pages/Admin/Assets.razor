@page "/admin/assets"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Assets</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Assets Management</h1>
        <p class="text-muted">Manage design assets (images, fonts, etc)</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/assets (paginated, sortable, filterable)
         - Create: POST /api/admin/assets with AssetDto
         - Update: PUT /api/admin/assets/{id} with AssetDto
         - Delete: DELETE /api/admin/assets/{id}
         - Export: CSV/Excel export
         - Grid Columns: AssetId, Name, AssetType, Url, UserId, FileSize, Status, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Name required, Url required, AssetType required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
