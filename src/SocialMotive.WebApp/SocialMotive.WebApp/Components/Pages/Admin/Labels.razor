@page "/admin/labels"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Labels</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Labels Management</h1>
        <p class="text-muted">Manage event and volunteer labels</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/labels (paginated, sortable, filterable)
         - Create: POST /api/admin/labels with LabelDto
         - Update: PUT /api/admin/labels/{id} with LabelDto
         - Delete: DELETE /api/admin/labels/{id}
         - Export: CSV/Excel export
         - Grid Columns: LabelId, Name, Color, Description, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Name required, Color required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
