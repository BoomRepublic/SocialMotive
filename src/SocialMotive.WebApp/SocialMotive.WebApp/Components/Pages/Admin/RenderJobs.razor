@page "/admin/render-jobs"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Render Jobs</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Render Jobs Management</h1>
        <p class="text-muted">Manage PNG rendering and export jobs</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/render-jobs (paginated, sortable, filterable)
         - Create: POST /api/admin/render-jobs with RenderJobDto
         - Update: PUT /api/admin/render-jobs/{id} with RenderJobDto
         - Delete: DELETE /api/admin/render-jobs/{id}
         - Export: CSV/Excel export
         - Grid Columns: JobId, TemplateId, UserId, Status, ProgressPercent, OutputUrl, ErrorMessage, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: TemplateId required, UserId required
         - Note: Some fields may be read-only (Status, ProgressPercent, OutputUrl)
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
