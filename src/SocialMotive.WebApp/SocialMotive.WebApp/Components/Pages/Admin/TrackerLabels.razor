@page "/admin/tracker-labels"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Tracker Labels</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Tracker Labels Management</h1>
        <p class="text-muted">Manage tracker label assignments</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/tracker-labels (paginated, sortable, filterable)
         - Create: POST /api/admin/tracker-labels with TrackerLabelDto
         - Update: PUT /api/admin/tracker-labels/{id} with TrackerLabelDto
         - Delete: DELETE /api/admin/tracker-labels/{id}
         - Export: CSV/Excel export
         - Grid Columns: TrackerLabelId, TrackerId, LabelId, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: TrackerId required, LabelId required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
