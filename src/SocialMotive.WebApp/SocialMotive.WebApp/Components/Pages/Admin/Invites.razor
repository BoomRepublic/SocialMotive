@page "/admin/invites"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Invites</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Invites Management</h1>
        <p class="text-muted">Manage invitation records</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/invites (paginated, sortable, filterable)
         - Create: POST /api/admin/invites with InviteDto
         - Update: PUT /api/admin/invites/{id} with InviteDto
         - Delete: DELETE /api/admin/invites/{id}
         - Export: CSV/Excel export
         - Grid Columns: InviteId, Email, Status, ExpirationDate, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Email required, valid email format
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
