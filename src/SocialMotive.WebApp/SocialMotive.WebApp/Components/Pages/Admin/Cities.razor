@page "/admin/cities"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Cities</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Cities Management</h1>
        <p class="text-muted">Manage city locations</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/cities (paginated, sortable, filterable)
         - Create: POST /api/admin/cities with CityDto
         - Update: PUT /api/admin/cities/{id} with CityDto
         - Delete: DELETE /api/admin/cities/{id}
         - Export: CSV/Excel export
         - Grid Columns: CityId, Name, Country, State, Latitude, Longitude, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Name required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
