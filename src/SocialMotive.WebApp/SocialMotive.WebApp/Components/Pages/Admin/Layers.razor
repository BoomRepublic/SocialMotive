@page "/admin/layers"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Layers</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Layers Management</h1>
        <p class="text-muted">Manage canvas layers in templates</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/layers (paginated, sortable, filterable)
         - Create: POST /api/admin/layers with LayerDto
         - Update: PUT /api/admin/layers/{id} with LayerDto
         - Delete: DELETE /api/admin/layers/{id}
         - Export: CSV/Excel export
         - Grid Columns: LayerId, TemplateId, Name, LayerType, ZIndex, Visible, Status, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Name required, TemplateId required, LayerType required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
