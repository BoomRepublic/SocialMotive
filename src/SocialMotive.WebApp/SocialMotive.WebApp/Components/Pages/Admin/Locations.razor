@page "/admin/locations"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Locations</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Locations Management</h1>
        <p class="text-muted">Manage GPS tracking locations</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/locations (paginated, sortable, filterable)
         - Create: POST /api/admin/locations with LocationDto
         - Update: PUT /api/admin/locations/{id} with LocationDto
         - Delete: DELETE /api/admin/locations/{id}
         - Export: CSV/Excel export
         - Grid Columns: LocationId, CityId, Name, Address, Latitude, Longitude, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Name required, CityId required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
