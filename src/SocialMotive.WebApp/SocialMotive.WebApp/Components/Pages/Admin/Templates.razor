@page "/admin/templates"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Templates</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Templates Management</h1>
        <p class="text-muted">Manage canvas templates in TrekkerGenerator</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/templates (paginated, sortable, filterable)
         - Create: POST /api/admin/templates with TemplateDto
         - Update: PUT /api/admin/templates/{id} with TemplateDto
         - Delete: DELETE /api/admin/templates/{id}
         - Export: CSV/Excel export
         - Grid Columns: TemplateId, Name, Description, UserId, Width, Height, Status, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Name required, Width required, Height required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
