@page "/admin/trackers"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Trackers</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Trackers Management</h1>
        <p class="text-muted">Manage volunteer tracker records</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/trackers (paginated, sortable, filterable)
         - Create: POST /api/admin/trackers with TrackerDto
         - Update: PUT /api/admin/trackers/{id} with TrackerDto
         - Delete: DELETE /api/admin/trackers/{id}
         - Export: CSV/Excel export
         - Grid Columns: TrackerId, UserId, Name, Description, Status, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Name required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
