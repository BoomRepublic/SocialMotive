@page "/admin/event-tasks"
@layout MainLayout
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "AdminFull")]

<PageTitle>Admin - Event Tasks</PageTitle>

<div class="admin-table-container">
    <div class="page-header">
        <h1>Event Tasks Management</h1>
        <p class="text-muted">Manage tasks within events</p>
    </div>

    <!-- TODO: Implement Telerik Grid component via MCP -->
    <!-- Features needed:
         - Read: GET /api/admin/event-tasks (paginated, sortable, filterable)
         - Create: POST /api/admin/event-tasks with EventTaskDto
         - Update: PUT /api/admin/event-tasks/{id} with EventTaskDto
         - Delete: DELETE /api/admin/event-tasks/{id}
         - Export: CSV/Excel export
         - Grid Columns: EventTaskId, EventId, Name, Description, SlotsNeeded, SlotsAvailable, Status, CreatedUtc, UpdatedUtc, Actions
         - Edit Mode: Inline or popup
         - Validation: Name required, SlotsNeeded required
    -->

    <div class="alert alert-info">
        <p><strong>Status:</strong> Grid component implementation pending. Use Telerik MCP patterns.</p>
    </div>
</div>

<style>
    .admin-table-container {
        padding: 2rem;
    }

    .page-header {
        margin-bottom: 2rem;
    }

    .page-header h1 {
        margin-bottom: 0.5rem;
    }
</style>
